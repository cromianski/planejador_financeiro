php_value display_errors Off

php_value mbstring.http_input auto

php_value date.timezone America/Sao_Paulo

# Deny access to the /src folder
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^src - [F]
</IfModule>